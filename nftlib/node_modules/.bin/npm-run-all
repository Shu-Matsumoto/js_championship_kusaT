#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../yarn-run-all/bin/npm-run-all/index.js" "$@"
else 
  exec node  "$basedir/../yarn-run-all/bin/npm-run-all/index.js" "$@"
fi
